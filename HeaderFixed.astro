---
// Fixed header wrapper used on non-index pages
import Header from './Header.astro';

interface Props {
  currentPage?: string;
}

const { currentPage = '' } = Astro.props;
---

<Header fixed includeStyles={true} currentPage={currentPage} />
